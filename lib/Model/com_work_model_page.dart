class ComWorkModelPage {
  String key, name, workClass, workTitle, timestamp, filename;

  ComWorkModelPage({
    required this.key,
    required this.name,
    required this.filename,
    required this.workClass,
    required this.workTitle,
    required this.timestamp,
  });
}
