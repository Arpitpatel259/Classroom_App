// ignore_for_file: file_names

class AboutDataModel {
  String name, enrollment, email, key;

  AboutDataModel(
      {required this.name,
      required this.enrollment,
      required this.email,
      required this.key});
}
